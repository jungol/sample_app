<li id="<%= feed_item.id %>">
  <%= link_to gravatar_for(feed_item.user, size: 70), feed_item.user %>
  <span class="user">
    <%= link_to feed_item.user.name, feed_item.user %>
  </span>
  <%= render 'shared/micropost_content', object: feed_item %>
</li>
